import React from 'react';
import { Layout } from '@components/molecules';
import { QuizPage } from '@components/content';

const Quiz = () => (
  <Layout>
    <QuizPage />
  </Layout>
);
export default Quiz;
